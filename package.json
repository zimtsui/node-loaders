{
  "name": "@zimtsui/node-loaders",
  "version": "0.0.3",
  "description": "",
  "exports": {
    ".": "./build/exports.js",
    "./unhandled": "./build/unhandled.js"
  },
  "engines": {
    "node": ">=24"
  },
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -b ./src/tsconfig.json",
    "clean": "rm -rf ./build",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "author": "Zim",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zimtsui/node-loaders.git"
  },
  "dependencies": {
    "@types/node": "^24.12.2",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  },
  "peerDependencies": {
    "typebox": "^1.1.31"
  }
}
